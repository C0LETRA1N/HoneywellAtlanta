'use strict';

angular.module('team6App.controllers', [])
.controller('HomeCtrl', ['$scope', '$rootScope', 
    function ($scope, $rootScope) {
       var vm = this;
       var googleMap = null;
       var posOptions = { timeout: 10000, enableHighAccuracy: true, maximumAge: 30000 };
       

    }
]);

angular.module('team6App.services', [])
.service('IBMBlueMixService', ['$http', function ($http) {
    
}]);